<% include ../partials/header %>
<div class="container">
    <div class="row">
            <h1 style="text-align: center;">Edit <%= campground.name %></h1>
            <div style="width: 30%; margin: 25px auto;">
                    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
                        <div class="form-group">
                                <input class="form-control" type="text" name="data[name]" value="<%= campground.name %>">
                        </div>
                        <div class="form-group">
                                <input class="form-control" type="text" name="data[image]" value="<%= campground.image %>">
                        </div>
                        <div class="form-group">
                                <input class="form-control" type="number" name="data[price]" value="<%= campground.price %>">
                        </div>
                        <div class="form-group">
                         /div>
                        <div       <input class="form-control" type="text" name="data[description]" name="<%= campground.description %>">
                        < class="form-group">
                                <button class="btn btn-primary">SUBMIT !</button>
                        </div>
                </form>
            </div>
    </div>
</div>

<% include ../partials/footer %>